! =============================================================================
!  Fabric-side BGP peering with Cilium on Kubernetes hosts.
!  Paste the matching block into each leaf. Safe to re-apply.
!
!  Per-leaf additions:
!    - K8S-VIP-POOL prefix-list: restricts what the host can advertise
!    - K8S-HOST-IN (permit pool), K8S-HOST-OUT (deny everything)
!    - maximum-paths ibgp 4 in VRF tenant-1 (ECMP across 4 leaves per VIP)
!    - neighbor under "router bgp 65000 / vrf tenant-1"
!
!  TO ADD ANOTHER VIP POOL LATER:
!    ip prefix-list K8S-VIP-POOL seq 20 permit <new-cidr> le 32
! =============================================================================


! ----- Leaf-1  (host: master 10.1.1.10 AS 65201, SVI 10.1.1.1) ----------------------------
conf t

ip prefix-list K8S-VIP-POOL seq 10 permit 192.168.100.0/24 le 32

route-map K8S-HOST-IN permit 10
  match ip address prefix-list K8S-VIP-POOL

route-map K8S-HOST-OUT deny 10

router bgp 65000
  vrf tenant-1
    address-family ipv4 unicast
      maximum-paths ibgp 4
    neighbor 10.1.1.10
      remote-as 65201
      description master (Cilium BGP)
      timers 10 30
      address-family ipv4 unicast
        send-community
        send-community extended
        route-map K8S-HOST-IN in
        route-map K8S-HOST-OUT out
        soft-reconfiguration inbound always

end
copy running-config startup-config


! ----- Leaf-2  (host: worker-1 10.1.2.10 AS 65202, SVI 10.1.2.1) ----------------------------
conf t

ip prefix-list K8S-VIP-POOL seq 10 permit 192.168.100.0/24 le 32

route-map K8S-HOST-IN permit 10
  match ip address prefix-list K8S-VIP-POOL

route-map K8S-HOST-OUT deny 10

router bgp 65000
  vrf tenant-1
    address-family ipv4 unicast
      maximum-paths ibgp 4
    neighbor 10.1.2.10
      remote-as 65202
      description worker-1 (Cilium BGP)
      timers 10 30
      address-family ipv4 unicast
        send-community
        send-community extended
        route-map K8S-HOST-IN in
        route-map K8S-HOST-OUT out
        soft-reconfiguration inbound always

end
copy running-config startup-config


! ----- Leaf-3  (host: worker-2 10.1.3.10 AS 65203, SVI 10.1.3.1) ----------------------------
conf t

ip prefix-list K8S-VIP-POOL seq 10 permit 192.168.100.0/24 le 32

route-map K8S-HOST-IN permit 10
  match ip address prefix-list K8S-VIP-POOL

route-map K8S-HOST-OUT deny 10

router bgp 65000
  vrf tenant-1
    address-family ipv4 unicast
      maximum-paths ibgp 4
    neighbor 10.1.3.10
      remote-as 65203
      description worker-2 (Cilium BGP)
      timers 10 30
      address-family ipv4 unicast
        send-community
        send-community extended
        route-map K8S-HOST-IN in
        route-map K8S-HOST-OUT out
        soft-reconfiguration inbound always

end
copy running-config startup-config


! ----- Leaf-4  (host: worker-3 10.1.4.10 AS 65204, SVI 10.1.4.1) ----------------------------
conf t

ip prefix-list K8S-VIP-POOL seq 10 permit 192.168.100.0/24 le 32

route-map K8S-HOST-IN permit 10
  match ip address prefix-list K8S-VIP-POOL

route-map K8S-HOST-OUT deny 10

router bgp 65000
  vrf tenant-1
    address-family ipv4 unicast
      maximum-paths ibgp 4
    neighbor 10.1.4.10
      remote-as 65204
      description worker-3 (Cilium BGP)
      timers 10 30
      address-family ipv4 unicast
        send-community
        send-community extended
        route-map K8S-HOST-IN in
        route-map K8S-HOST-OUT out
        soft-reconfiguration inbound always

end
copy running-config startup-config

